{% extends 'base.html.twig' %} {% block title %}Users | Megs Art{% endblock %} 
{% block pagemenu %}
    <div class="page">
        <ul class="page-menu">
            <li><a class="darkgrey" href="/">Home</a></li>
            <span class="grey">»</span>
            <li><a class="grey"> {{ app.request.attributes.get('_route') }}</a></li>
        </ul>
    </div>    
{% endblock %}
{% block body %}
    <div id="content-top">
        <div id="content-desc">
            <h1 class="content-title">{{pageTitle}}</h1>
            <p class="content-description">{{pageDesc}}
            </p>
        </div>
    </div>  
    <div id="content">
        {% for user in users %}
            <div class="publications">
                <p>Username: {{user.userName}}<p> 
                <p>Email: {{user.userEmail}}<p> 
                <div>   
                    <button onclick="location.href = './users/delete/{{user.id}}'">Delete</button>  
                </div>
            </div>
        {% endfor %}
    </div>
{%endblock%}
